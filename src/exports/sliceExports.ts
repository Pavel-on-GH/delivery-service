export { setCategoryValue, setSortValue, selectFilter } from '../redux/slices/filterSlice';
export { setProducts, selectProduct, fetchProducts } from '../redux/slices/productsSlice';
export { setSearchValue, selectSearch } from '../redux/slices/searchSlice';
export {
  addProduct,
  removeProduct,
  clearBasket,
  dicrement,
  increment,
  selectBascket,
} from '../redux/slices/basketSlice';
